<!DOCTYPE html>
<html>
  <head>
    <title>Papatomo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="" href="/"><%= image_tag('PAPATOMO4 (1).png', size: '200x60') %></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
    <%# ユーザーログイン後のヘッダー　%>
            <% if end_user_signed_in? %>
            　<li class="nav-item">

              <li class="nav-item">
                <%= link_to 'ホーム', posts_path, class: 'nav-link mr-2 btn btn-outline-secondary text-secondary' %>
              </li>

              <li class="nav-item">
                <%= link_to 'マイページ', my_page_users_path, class: 'nav-link mr-2 btn btn-outline-secondary text-secondary' %>
              </li>

              <li class="nav-item">
                <%= link_to '新規投稿', new_post_path, class: 'nav-link mr-2 btn btn-outline-secondary text-secondary' %>
              </li>

              <li class="nav-item">
                <%= link_to 'ログアウト', destroy_end_user_session_path, method: :delete, class: 'nav-link btn btn-outline-secondary text-secondary' %>
              </li>
  <%# 管理者ログイン後のヘッダー　%>
            <% elsif admin_signed_in? %>
              <li class="nav-item">
                <%= link_to '会員一覧', admins_customers_path, class: 'nav-link ml-4 btn btn-outline-secondary text-secondary' %>
              </li>
              <li class="nav-item">
                <%= link_to 'ジャンル一覧', admins_genres_path, class: 'nav-link ml-4 btn btn-outline-secondary text-secondary' %>
              </li>
              <li class="nav-item">
                <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete, class: 'nav-link ml-4 btn btn-outline-secondary text-secondary' %>
              </li>
            <%# ログイン前のヘッダー(ユーザー、管理者共通)　%>
            <% else %>
              <li>
                <%= link_to '新規登録', new_end_user_registration_path, class: 'nav-link btn btn-outline-secondary text-secondary' %>
              </li>
              <li>
                <%= link_to "ログイン", new_end_user_session_path ,class: 'nav-link ml-4 btn btn-outline-secondary text-secondary' %>
              </li>
              <li>
                <%= link_to 'ゲストログイン（閲覧用）', end_users_guest_sign_in_path, class: "nav-link ml-4 btn btn-outline-secondary text-secondary", method: :post %>
              </li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
      <div class="offset-8"><%= render 'end_users/searches/search', range: @range %></div>
    </header>
    <%= yield %>
  </body>
</html>
